# LawnStringsJsonMap

Representa los datos de un archivo *LawnStrings* en formato JSON.

## Constructores

▶ `LawnStringsJsonMap()`

Crea una nueva instancia de la clase `LawnStringsJsonMap`, inicializando la clase de objeto, los datos y los alias correspondientes.

### Ejemplo

```csharp
LawnStringsJsonMap jsonMap = new LawnStringsJsonMap();
```