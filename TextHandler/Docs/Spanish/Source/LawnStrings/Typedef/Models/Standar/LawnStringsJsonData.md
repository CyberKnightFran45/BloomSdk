# LawnStringsJsonData

Representa los datos de un archivo *LawnStrings* en formato JSON.

## Constructores

▶ `LawnStringsJsonData()`

Crea una nueva instancia de la clase `LawnStringsJsonData`, inicializando el objeto con la clase `LawnStringsData` y configurando los alias correspondientes.

### Ejemplo

```csharp
LawnStringsJsonData jsonData = new LawnStringsJsonData();
```